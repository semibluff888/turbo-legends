# 多人模式用户系统 V1（修订版）

## 目标

- 用户系统只作用于多人模式；单人模式不创建用户、不读取资料、不累计数据。
- 首次进入多人模式自动创建游客账号，并通过长期 HttpOnly Cookie 恢复。
- 使用 SQLite 持久化等级、经验、Rating、比赛、完赛、逃跑、领奖台及各地图最快完赛时间。
- 本期不实现排行榜、正式账号登录、跨设备恢复或后台用户管理工具。

## 用户身份与存储

- 使用 `node:sqlite`，数据库路径由 `USER_DB_PATH` 配置，Docker 将 `/data` 挂载为持久化数据卷。
- SQLite 启用 WAL、外键、`busy_timeout=5000` 和 `synchronous=NORMAL`。
- 数据表包括：
  - `users`：用户 ID、昵称、经验、Rating、比赛/完赛/逃跑及冠亚季军统计。
  - `user_sessions`：会话令牌摘要、用户 ID、创建、最后使用和过期时间。
  - `races`：比赛 ID、地图、房间类型、真人数量和结算时间。
  - `race_user_starts`：倒计时开始时幂等登记真人参赛者，保证场次可在中途重启前持久化。
  - `race_user_results`：正式名次、真人名次、自然完赛、逃跑、完赛时间、经验及 Rating 变化。
  - `user_track_records`：每名用户在每张地图的最快自然完赛时间。
- `race_user_results` 使用 `UNIQUE(race_id, user_id)`，结算必须幂等。
- `completionRate = finishes / races`、`escapeRate = escapes / races`，无比赛时为 `0`，两者均不入库。
- 会话令牌为 32 字节随机值，数据库只保存 SHA-256 摘要；Cookie 使用 `HttpOnly; SameSite=Strict; Path=/`，HTTPS 环境增加 `Secure`。
- 同一用户只能拥有一个活动房间参与者。
- 用户关联表使用级联删除。V1 不提供删除 API/CLI；确有需要时可停服后按 `userId` 手工删除。
- 测试阶段默认不限制游客账号创建；需要时可通过 `GUEST_CREATION_LIMIT` 设置同一 IP 在窗口内的创建上限，有效 Cookie 恢复不计入限额。

## API 与多人协议

- `POST /api/user/session`：恢复现有游客账号或创建新账号。
- `GET /api/me`：读取当前用户资料。
- `PATCH /api/me`：更新昵称。
- 资料响应包含：
  - `user`：昵称、等级、总经验、当前等级经验、下一级门槛、Rating。
  - `stats`：比赛、完赛、完赛率、逃跑、逃跑率、冠军、亚军、季军。
  - `trackBestTimes`：所有地图的 `{ trackId, finishTimeMs|null }`。
- localStorage 昵称仅作为首次游客账号的迁移值，之后服务端资料为权威来源。
- 多人客户端必须先初始化游客会话，再连接 `/ws`；无有效会话的 Upgrade 返回 401。
- 协议升级到 v5：房间操作不信任客户端昵称，`welcome` 返回用户资料摘要，并增加私有 `user_progression` 消息。
- `userId` 不广播到大厅、房间名单或公开赛果；恢复房间时还必须匹配认证用户。

## 比赛、完赛与逃跑

- 比赛成功进入倒计时后，真人参与者累计一次比赛；加载阶段取消不统计。
- 自然冲线前主动离开比赛，或自然冲线前断线并超过 30 秒恢复窗口，记一次逃跑。
- 玩家自然冲线后立即锁定本场成绩；此后主动离开或断线都不再记逃跑，仍正常累计完赛、奖励和地图纪录。
- 尚未自然冲线的玩家在 30 秒内重连不算逃跑；AI 接管时间不影响统计。
- 比赛结果产生后再断线或离开，不追记该场逃跑。
- 若比赛先结束而未冲线玩家的恢复窗口尚未到期，普通结果立即展示，成长数据等待重连或窗口到期后再结算。
- 逃跑用户：比赛和逃跑各加一，不增加完赛、领奖台、经验、地图纪录或 Rating，也从所有人的 Rating 对手池剔除。
- 未逃跑但赛后倒计时内未自然冲线：计比赛、不计完赛和地图纪录，正式名次、领奖台、经验和 Rating 仍正常计算。
- 自动排位结果公开 `finishTime=null` 并显示 DNF。

## 经验、等级与 Rating

- 经验：未逃跑 20 XP；自然冲线额外 10 XP；正式总排名冠/亚/季军额外 30/20/10 XP。
- 等级由经验派生，到达等级 `L` 的累计门槛为 `50 × (L-1) × L`。
- 冠亚季军按包含 AI 的正式总排名统计，不向真人递补。
- Rating 只比较未逃跑真人；公开房和私人房均计分。
- 初始 Rating 1000，K=32；有效真人少于两人时不变化，最低为 0。
- 两两 Elo：`expected = 1 / (1 + 10 ^ ((opponentRating - userRating) / 400))`，最终变化为 `round(32 × average(actual - expected))`。
- 赛果、统计、经验、Rating 与地图纪录在同一事务内提交；失败不能影响普通比赛结果展示。

## 多人界面

- 多人大厅账号区域显示昵称、等级和 Rating，可打开资料弹窗。
- 资料弹窗展示经验进度、Rating、比赛/完赛/逃跑及对应比率、冠亚季军和各地图最快完赛时间。
- 多人结算页展示经验变化、Rating 增减、升级、新纪录，以及等待断线判定时的同步状态。
- 新增界面补齐中英文文案和移动端布局。

## 验收测试

- 游客创建、Cookie 恢复、昵称更新、过期会话和重启持久化。
- 自然完赛、超时 DNF、领奖台、经验、等级、地图纪录和幂等结算。
- 冲线前主动逃跑、冲线后离开锁定成绩、断线重连、恢复超时、延迟结算和逃跑/完赛率。
- Elo 过滤 AI/逃跑者、单一有效真人、不同 Rating 对手和 Rating 下限。
- WebSocket 认证、用户与恢复会话匹配、同账号单一活动房间。
- 外键级联删除、多人 UI、协议 v5、Docker 数据卷和现有单人/多人回归测试。
