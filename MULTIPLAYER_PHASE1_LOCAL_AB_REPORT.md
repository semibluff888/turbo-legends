# 多人网络第一阶段本地 A/B 测试报告

> 测试日期：2026-08-03  
> 优化前：Git `HEAD`（`eab32fe`）的隔离导出副本  
> 优化后：当前未提交工作区  
> 环境：Windows、Node.js v24.12.0、单机回环网络

## 结论

第一阶段优化在本地关键合成场景中已经表现出明确收益，而且没有通过降低协议版本、物理频率或快照频率换取性能：

- 与 Lobby 摘要无关的房间操作不再产生 Lobby 流量，本场景从 59,520B 降为 0。
- 24 个连接的重连风暴中，观察客户端收到的 `server_stats` 流量减少约 97.9%。
- 12 个并发私房密码计算期间，WebSocket ping p95 从 295.25ms 降至 1.15ms，说明异步 scrypt 显著消除了事件循环阻塞。
- 80 场无人接收但保留恢复能力的比赛中，5 秒进程 CPU 时间从 46ms 降至 16ms，约减少 65.2%。
- 有正常接收者时，优化前后客户端快照频率均约为 20Hz，没有观察到游戏网络频率退化。
- 同一未修改 JS 文件的首次静态传输从 17,528B 降至 4,247B，约减少 75.8%；命中 ETag 后为 304/0B。

## 核心结果

| 场景 | 优化前 | 优化后 | 本地效果 |
| --- | ---: | ---: | ---: |
| 10 个 Lobby 观察者、24 次无关 `ready` 操作：Lobby 消息 | 240 | 0 | -100% |
| 上述 Lobby 流量 | 59,520B | 0B | -100% |
| 10 个观察者、24 个连接快速连入/断开：`server_stats` 消息 | 480 | 10 | -97.9% |
| 上述 `server_stats` 流量 | 35,040B | 730B | -97.9% |
| 12 个并发私房创建完成时间 | 348.86ms | 179.80ms | -48.5% |
| 私房认证期间 ping p95 | 295.25ms | 1.15ms | -99.6% |
| 私房认证期间事件循环延迟 p99 | 320.34ms | 16.38ms | -94.9% |
| 1000 个等待房间：事件循环利用率 | 0.79% | 0.39% | 约 -50.6% |
| 80 场无人接收的可恢复比赛：5 秒 CPU 时间 | 46ms | 16ms | 约 -65.2% |
| 上述场景事件循环利用率 | 1.94% | 0.75% | 约 -61.3% |
| 6 场活动比赛、12 个接收者：每客户端快照频率 | 20.20Hz | 20.16Hz | 基本一致 |
| 静态 JS 首次传输 | 17,528B，identity | 4,247B，Brotli | -75.8% |
| 静态 JS 条件请求 | 200 / 17,528B | 304 / 0B | 命中后无正文 |
| 1KiB Range 请求 | 200 / 17,528B | 206 / 1,024B | 仅传请求范围 |

所有场景的服务端错误计数均为 0。

## 场景说明

### Lobby 与连接放大

- 10 个稳定客户端停留在 Lobby。
- 创建一个双人房间后，连续执行 24 次不会改变 Lobby 摘要的 `ready` 操作。
- 另用 24 个临时连接快速连接并断开，观察稳定客户端收到的 `server_stats` 数量和字节数。
- 结果验证了 Lobby 摘要字段比较、100ms 合并以及 `server_stats` 250ms 合并均已生效。

### 私房认证响应性

- 同时创建 12 个有密码的私房，并以 20ms 间隔通过另一条 WebSocket 连接发送 ping。
- 优化前同步 scrypt 占用主事件循环；优化后计算总 CPU 工作量仍然存在，但移到异步有界队列，不再长期阻塞比赛和网络事件。
- 优化后采样窗口更短，因此瞬时 CPU 百分比不适合直接和优化前比较；更有意义的是认证完成时间、事件循环利用率与 ping 延迟。

### 调度与快照

- 1000 个等待房间用于观察 60Hz 全房扫描拆分后的空闲调度开销。
- 80 场比赛在没有接收者时仍继续权威模拟，模拟玩家处于可恢复窗口；优化后跳过周期快照构建。
- 另用 6 场比赛和 12 个正常接收者验证快照仍维持约 20Hz。

### 静态资源

- 请求 Git 基线与当前工作区内容相同的 `/src/game/race-simulation.js`，避免用不同文件大小夸大压缩收益。
- 分别验证 Brotli、ETag/304、HEAD 和单段 Range。

## 限制与解读

- 这是单机回环网络上的合成 A/B，不包含公网丢包、真实 RTT、TLS、容器资源限制、反向代理或生产玩家行为。
- Windows 的进程 CPU 采样存在约 15～16ms 粒度；非常轻的场景可能显示为 0，因此报告只对压力足够的 CPU 场景给出降幅。
- 1000 个等待房间的单次 RSS 结果波动较大，优化前为 68.98MiB、优化后为 85.94MiB；与此同时 heap used 从 14.58MiB 降至 11.82MiB。RSS 会受 V8 预留页、GC 时机和指标收集器影响，本次单样本不足以判断内存趋势。
- 核心 Lobby、重连、认证和静态资源场景在测试脚本验证轮与正式记录轮方向一致；报告表格采用正式记录轮数据。
- 生产上线前后对比仍必须保留：建议使用 `/api/metrics` 和结构化日志至少采集同一时间段的带宽、tick p99、事件循环 p99、Lobby 字节数、快照跳过率、RSS 与并发量。

## 验收判断

- 本地关键优化：通过。
- 协议 v3：保持不变。
- 权威物理 120Hz、服务器 tick 60Hz、快照 20Hz：保持不变；外部实测快照约 20Hz。
- 无关房间操作不产生 Lobby 流量：通过。
- 认证期间网络响应性：显著改善。
- 生产容量结论：尚不能仅凭本报告下结论，仍需上线前后真实指标对比。
