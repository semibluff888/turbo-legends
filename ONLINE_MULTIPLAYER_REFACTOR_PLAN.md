# Turbo Legends 多人在线重构计划

> 状态：Lobby / Room v2 已实施
> 日期：2026-07-30
> 目标版本：多人在线模式 v2

## v2 Lobby / Room 更新（2026-07-30）

当前 v2 在不改变八车权威比赛、AI 接管和内存房间架构的前提下，
采用以下产品规则：

- 新增实时 Multiplayer Lobby；公开房和密码保护的好友房都会显示，
  支持搜索、直接加入和仅匹配可加入公开房的 Quick Match。
- Room 与 Lobby 成为不同概念；房间状态机使用 `waiting`，赛后操作为
  `return_room`，离开房间会回到 Lobby。
- 房间创建时设置名称、`public` / `private` 类型和 2–8 个真人席位；
  好友房密码只保存随机盐与 scrypt 摘要。
- 昵称允许重复，仅作为展示文本。首次使用从内置昵称库随机选择，
  `participantId` 始终是权限、重连、输入和结果归属的唯一身份。
- WebSocket JSON 协议升级到 v2，新增 `enter_lobby`、`lobby_state`、
  `quick_match`、`return_room` 和 `leave_room`。
- 在线结算页仅保留“返回房间”，不再提供直接离开房间操作。

## 1. 目标与成功标准

在保留现有单人模式、纯 JavaScript 模拟层和无前端构建步骤的前提下，为 Turbo Legends 增加可部署到公网的好友房联机模式。

成功标准：

- 玩家可以在实时 Lobby 中搜索、创建或加入公开房和好友房，也可以快速匹配公开房。
- 每局支持 2–8 名真人，空位由 AI 补齐到 8 辆车。
- 服务端权威计算车辆、物品、圈数、排名和比赛结果，客户端不能直接修改比赛状态。
- 两个或更多浏览器能看到一致的车辆、物品、排名和最终结果。
- 公网延迟下，本地车辆通过预测与校正保持可接受的操控反馈，远端对象平滑移动。
- 意外掉线后由 AI 接管，玩家可在 30 秒内重连并收回控制。
- 一局结束后保留原房间和成员，可调整设置后继续重赛。
- 现有单人模式和全部自动化测试保持可用。

## 2. 已确认的产品规则

### 2.1 房间与身份

- Lobby 实时显示公开房和密码保护的好友房；六位房间码继续用于搜索和邀请链接。
- 玩家使用匿名昵称，不做账号、数据库、跨设备战绩或排行榜。
- 昵称保存在浏览器本地；房间、成员和比赛状态仅保存在服务端内存。
- 房间码为 6 位，大小写不敏感，并排除容易混淆的字符。
- 昵称允许重复，只用于展示；服务端以 `participantId` 处理身份、权限、重连、输入和结果归属。
- 房间创建时设置 2–8 个真人席位，处于重连宽限期的席位仍占用容量。

### 2.2 Room 准备房间

- 房主选择赛道和 AI 难度，圈数继续使用赛道自带配置。
- 每个角色只能被一名真人占用；AI 从剩余角色中补位。
- 修改自己的角色会清除自己的准备状态。
- 房主修改赛道或 AI 难度会清除所有玩家的准备状态。
- 至少两名真人在线且所有人均已准备后，房主才能开始比赛。
- 房主掉线时，房主权限转移给当前最早加入的在线玩家。

### 2.3 开赛与阵容

- 房主开始比赛后，房间先进入加载阶段。
- 服务端发送赛道、阵容、难度、随机种子和本局标识；客户端创建场景后回复加载完成。
- 加载等待上限为 10 秒。超时玩家由 AI 接管；若在线真人因此少于两名，则取消开赛并返回 Room。
- AI 补齐后始终保持 8 辆车。
- 每局使用新的服务端随机种子，并确定性打乱发车顺序，避免房主或加入顺序获得固定优势。
- 倒计时开始后禁止新玩家加入；只有本局原玩家可以凭重连令牌恢复席位。

### 2.4 掉线、离开与重赛

- 意外掉线时，AI 立即接管车辆。
- 玩家持有效重连令牌时，可在 30 秒内重连并收回控制。
- 超过 30 秒后，该车辆由 AI 跑完全程；比赛返回 Room 时从成员列表移除。
- 主动离开视为放弃重连，车辆仍由 AI 完成本局，避免影响其他玩家。
- 比赛结束后保留房间码、在线成员、角色和房间设置，但清除所有准备状态。
- 每位玩家可独立返回 Room；若没有操作，结果展示 30 秒后自动返回。
- 空房间保留 60 秒供短暂断线重连；空闲 Room 15 分钟后自动回收。

### 2.5 当前版本明确不包含

- 账号、登录、云端战绩和排行榜。
- 聊天、语音、好友系统和邀请平台集成。
- 观战模式。
- 比赛开始后的新玩家加入或中途接管 AI。
- 多 Node 实例之间的房间迁移或共享状态。

## 3. 总体架构

### 3.1 权威服务端模型

Node 服务端是比赛状态的唯一权威来源：

- 客户端只提交驾驶操作。
- 服务端计算物理、碰撞、漂移、增益、物品抽取、物品命中、圈数、排名和结果。
- 客户端提交的位置、速度、物品、排名、时间步或完赛结果一律不可信。
- 所有玩家接收同一权威比赛状态，客户端预测只用于展示和操控反馈。

运行频率：

- 核心物理继续使用现有 120 Hz 固定步长。
- 服务端网络调度以 60 Hz 运行，每个网络 tick 执行两次 120 Hz 模拟步。
- 客户端最多以 60 Hz 发送输入。
- 服务端以 20 Hz 发送完整状态快照。
- 服务端使用单调时钟和有上限的追赶循环，不接受客户端提供的 dt。

### 3.2 核心模拟重构

从当前单玩家导向的 RaceDirector 中抽出通用 RaceSimulation。

建议接口：

```js
new RaceSimulation(track, {
  roster,
  difficulty,
  laps,
  seed,
  mode,
});

simulation.update(dt, controlsByKartIndex);
simulation.setController(kartIndex, controllerKind);
```

其中 roster 的每一项至少包含：

```js
{
  participantId,
  displayName,
  characterId,
  controllerKind, // 'human' | 'ai' | 'takeover-ai'
}
```

核心要求：

- RaceSimulation 支持按 kart 索引输入多名真人的操作。
- 真人、常规 AI 和掉线接管 AI 可以在不重建比赛的情况下切换。
- 所有席位预先创建独立 AI 驱动器，掉线时只切换控制权。
- 权威比赛状态只使用 countdown、racing、results。
- “当前客户端的玩家已完赛”属于客户端视图状态，不进入全局权威状态机。
- Kart.isPlayer 只表示当前客户端的本地展示对象，不再承担模拟层语义。
- 常规在线 AI 以仍在线真人的中位进度作为橡皮筋参考。
- 接管真人车辆的 AI 不获得橡皮筋速度奖励，避免断线带来竞赛收益。

### 3.3 保持单人兼容

现有公共调用方式继续保留：

```js
new RaceDirector(track, options);
director.update(dt, playerControls);
```

RaceDirector 改为 RaceSimulation 的单人适配器，并继续提供现有的 player、karts、items、standings、state、countdown 和 elapsed 等访问方式。

现有单人规则保持不变：

- 玩家仍从后排发车。
- 其他位置由 7 名不同角色的 AI 驾驶。
- autopilot 继续服务于无界面比赛测试。
- 当前渲染、HUD、音频和输入模块不直接依赖服务端代码。

### 3.4 随机数隔离

将当前共享随机流拆分为：

- 阵容与发车位随机流。
- ItemSystem 专用随机流。
- 每辆 AI 的独立随机流。

所有子随机流从本局 seed 和稳定命名空间派生。启用掉线接管 AI 不得消费物品随机流，也不得改变其他 AI 的行为序列。

## 4. 服务端设计

### 4.1 HTTP 与 WebSocket

- 保留现有静态资源服务器。
- 在同一个 HTTP server 上通过 Upgrade 挂载 /ws。
- 浏览器根据当前页面协议自动连接 ws:// 或 wss:// 的同源地址。
- 服务端使用 ws 依赖；前端继续使用浏览器原生 WebSocket。
- server.mjs 继续作为 npm start 的入口，并组合静态服务、WebSocket 服务和房间管理器。
- 服务端专用模块放在不属于静态公开目录的 server/ 目录中。

新增健康检查：

```text
GET /healthz
```

返回进程存活时间、当前房间数、比赛数和连接数，不暴露房间码、昵称或重连令牌。

### 4.2 房间状态机

```text
waiting -> loading -> countdown -> racing -> results -> waiting
```

RoomManager 负责：

- 生成和回收房间码。
- 成员、房主、准备状态和唯一角色锁。
- 赛道、难度和房间状态。
- 加载确认和超时。
- 重连令牌与宽限期。
- 创建、运行和销毁服务端比赛会话。
- 赛后返回 Room 和空闲房间清理。

### 4.3 输入处理

客户端输入消息包含：

```js
{
  type: 'input',
  v: 2,
  raceId,
  seq,
  useItemSeq,
  throttle,
  brake,
  steer,
  drift,
  lookBack
}
```

处理规则：

- seq 必须单调递增，旧输入直接忽略。
- throttle 和 brake 夹取到 0..1。
- steer 夹取到 -1..1。
- drift 和 lookBack 转换为布尔值。
- useItemSeq 仅在本地按下物品键的上升沿递增。
- 服务端观察到新的 useItemSeq 时，只在一个固定模拟步中产生 useItem=true。
- 服务端每个 60 Hz tick 采用最新有效移动输入，必要时跳过已经过时的中间输入。
- 1.5 秒没有有效输入时临时交由接管 AI；有效输入恢复后允许当前连接收回控制。
- 服务端在快照中返回当前玩家最后已应用的输入 seq，用于客户端预测校正。

## 5. WebSocket 协议 v2

所有应用层消息使用带 `v: 2` 的 JSON 对象。

### 5.1 客户端到服务端

- enter_lobby：订阅实时 Lobby 房间列表。
- create_room：使用昵称、房间名、类型、容量和可选密码创建 Room。
- join_room：使用房间码、昵称和可选密码加入 Room。
- quick_match：随机加入一个公开、等待中且未满的 Room。
- resume：使用房间码、participantId 和私密 resumeToken 恢复会话。
- select_character：请求锁定一个未占用角色。
- set_room：房主修改 trackId 或 difficulty。
- set_ready：设置自己的准备状态。
- start_race：房主请求开赛。
- race_loaded：客户端完成赛道和车辆场景创建。
- input：发送驾驶状态和物品操作序号。
- return_room：赛后返回当前 Room。
- leave_room：主动离开 Room、放弃重连并返回 Lobby。
- ping：浏览器侧延迟和时钟偏移测量。

### 5.2 服务端到客户端

- welcome：连接和协议确认。
- lobby_state：可展示房间的实时全量摘要，不包含成员 ID、密码或恢复凭据。
- room_state：完整 Room 状态，包含房间元数据、房主、成员、设置和准备状态。
- prepare_race：本局 raceId、seed、赛道、难度、圈数和完整阵容。
- snapshot：权威比赛快照和本客户端的 input ack。
- race_events：带全局递增 eventId 的音频与 VFX 事件。
- race_results：最终权威排名、时间和单圈成绩。
- error：稳定错误码及可展示消息。
- pong：服务端时间和当前 tick。

### 5.3 快照内容

每个 snapshot 至少包含：

- 协议版本、raceId、服务端 tick、客户端 input ack。
- 比赛状态、倒计时、elapsed、圈数。
- 8 辆 kart 的身份、角色、连接/控制状态。
- 位置、朝向、速度、悬空状态和视觉姿态。
- 转向、制动、漂移、增益和状态效果。
- 当前物品、使用次数、抽奖状态。
- 赛道进度、圈数、排名、完赛时间和单圈成绩。
- 所有活动投射物和障碍物的稳定 id、类型、位置、朝向和必要状态。
- 物品箱的 active 和 respawnAt 状态。

状态快照允许在客户端严重积压时跳过，因为下一份完整快照可以替代旧状态。race_events 不可静默跳过；客户端按 eventId 去重。

## 6. 客户端设计

### 6.1 统一比赛会话

主循环改为依赖统一会话接口，而不是直接依赖 RaceDirector：

```js
{
  track,
  karts,
  player,
  items,
  state,
  countdown,
  elapsed,
  laps,
  standings,
  update(dt, controls),
  dispose()
}
```

实现两个会话：

- LocalRaceSession：包装单人 RaceDirector。
- OnlineRaceSession：管理 WebSocket、状态镜像、输入发送、重连、预测和插值。

现有渲染、HUD、音频和摄像机继续读取同形状的 Kart 和比赛状态对象。

### 6.2 状态镜像与插值

- OnlineRaceSession 为每辆车创建本地 Kart 镜像。
- 只有当前客户端自己的 kart 设置 isPlayer=true。
- 真人 kart 的 name 使用玩家昵称；AI 使用角色名称。
- 非运动状态使用最新权威快照。
- 远端车辆、投射物和障碍物使用约 100 ms 的快照缓冲插值。
- yaw 使用最短角度插值。
- 重生、传送、实体首次出现或误差过大时直接吸附。
- 物品箱状态写回客户端 Track 实例，继续复用现有场景动画。

### 6.3 本地预测与校正

- 本地车辆使用相同的 stepKartPhysics，以 120 Hz 预测自己的移动。
- 每个 60 Hz 输入命令对应两个预测物理步，并保留在未确认输入历史中。
- 收到 snapshot 后，以权威本地 kart 状态作为新基准，丢弃 seq 小于等于 ack 的输入，再重放未确认输入。
- 位置误差不超过 4 米且权威状态未改变时，通过约 100 ms 的视觉偏移平滑修正。
- 超过 4 米、发生重生、受击或状态机不一致时立即吸附。
- 预测阶段不产生权威音效、VFX、物品命中、抽奖或比赛结果。
- 所有游戏事件只从 race_events 消费，避免预测回滚造成重复播放。

### 6.4 重连

- 匿名昵称保存在 localStorage。
- participantId 和 resumeToken 只保存在当前页面内存，不进入浏览器存储、邀请链接或日志。
- 意外断开后采用短间隔指数退避重连，总窗口不超过 30 秒。
- 重连成功后先应用完整房间状态或比赛快照，再恢复输入发送和预测。
- 页面刷新会重新进入 Lobby；敏感会话凭据不会跨刷新恢复。

## 7. UI 与交互

### 7.1 标题页与多人入口

- 移除多人按钮的 COMING SOON 标记。
- 多人入口进入 Lobby；昵称编辑、搜索、快速开始和创建房间都在 Lobby 中完成。
- 支持 `?room=CODE` 查询参数，打开邀请链接后在 Lobby 定位对应房间。
- 文本输入获得焦点时暂停菜单快捷键处理，防止 WASD 或 Enter 同时触发驾驶/菜单逻辑。

### 7.2 Room

Room 显示：

- 房间码和复制邀请链接操作。
- 按 `maxPlayers` 渲染的真人席位、昵称、角色、房主、连接状态和准备状态。
- 可用角色网格及已锁定提示。
- 当前赛道和 AI 难度。
- 房主专属设置和开赛按钮。
- 普通玩家准备/取消准备按钮。
- 稳定的错误提示，例如房间不存在、已满、角色已占用或比赛已开始。

### 7.3 在线比赛

- 在线 Esc 菜单只是本地覆盖层，不暂停服务端比赛或快照处理。
- 打开菜单时立即发送空驾驶操作。
- 在线暂停菜单移除 RESTART，改为 LEAVE ROOM。
- 设置和帮助页面可以打开，但后台继续处理网络和渲染。
- 页面隐藏时发送空操作，不暂停服务端；恢复显示后用最新快照校正。

### 7.4 在线结果

- 使用服务端 race_results 展示权威成绩。
- 所有玩家仅显示 RETURN TO ROOM，不提供离开房间按钮。
- 每位玩家可独立返回；30 秒后仍停留在结果页的玩家自动返回。
- 返回 Room 时复用原房间，清除准备状态，并允许修改角色、赛道和难度。

## 8. 安全、稳定性与部署

- WebSocket 默认要求同源 Origin；通过 ALLOWED_ORIGINS 环境变量配置额外允许来源。
- 客户端单条消息上限 16 KiB。
- 每连接最多接收 90 条应用消息/秒。
- 对创建和房间码尝试单独限流。
- 所有输入字段执行类型、范围、长度和枚举校验。
- 拒绝非房主修改设置或开始比赛。
- 拒绝比赛中新增成员、重复角色、过期 raceId 和乱序输入。
- 服务端定期执行 WebSocket ping/pong；失活连接会被终止并进入 AI 接管。
- 禁用不必要的 WebSocket 压缩，避免额外 CPU 和内存压力。
- 快照发送积压超过阈值时跳过中间快照；持续积压的客户端按慢连接断开。
- 支持 HOST、PORT 和 ALLOWED_ORIGINS 环境变量。
- TLS 及公网 wss 由部署平台或反向代理终止，并正确转发 Upgrade。
- SIGTERM 时停止接受新连接，并以可重试关闭码通知现有客户端。

## 9. 测试计划

### 9.1 保持现有基线

- 当前 80 项测试必须继续通过。
- npm run check 必须成功导入全部浏览器与模拟模块。
- 单人完整比赛的确定性、完赛、圈数、排名和 reset 测试保持覆盖。

### 9.2 核心模拟测试

- 两名或更多真人的输入只影响各自 kart。
- AI 正确补齐到 8 辆车且角色不重复。
- 相同 seed、阵容和输入产生相同结果。
- 各随机流互不干扰。
- 中途启用或关闭接管 AI 不破坏物品随机序列。
- 多名真人分别完赛时，全局状态直到所有车辆完成或超时后才进入 results。
- 在线 AI 的中位真人进度参考及接管 AI 的无橡皮筋规则正确。

### 9.3 房间与协议测试

- 公开/好友房创建、密码校验、加入、满员、无效房间码和重复昵称。
- 唯一角色锁及掉线宽限期释放。
- 房主权限、房主迁移和非房主越权拒绝。
- 设置变化与准备状态重置。
- 至少两名真人和全员准备的开赛条件。
- 加载确认、10 秒超时和真人不足时取消开赛。
- 比赛中拒绝新玩家。
- 主动离开、意外掉线、AI 接管、30 秒内重连及超时。
- 赛后返回 Room 和房间状态复用。
- 消息大小、频率、类型、范围、raceId 和输入顺序校验。

### 9.4 状态同步测试

- 快照序列化和应用后的关键状态一致。
- 投射物、障碍物和物品箱完整同步。
- 事件 id 去重且不会因跳过快照丢失。
- yaw 跨越 -π/π 时正确插值。
- 重生和大误差触发吸附。
- 本地预测在收到 ack 后正确丢弃并重放输入。
- useItemSeq 在跳过移动输入时仍只触发一次物品使用。

### 9.5 端到端与人工验收

- 启动临时服务端，用两个 WebSocket 客户端完成创建、加入、准备、加载和开赛。
- 两端收到一致的服务端 tick、车辆、物品、排名和结果。
- 其中一个客户端掉线后 AI 接管，并能在 30 秒内重连收回控制。
- 测试比赛中断线重连、刷新后回 Lobby、打开暂停菜单、隐藏页面和主动离开。
- 使用约 150 ms 延迟和抖动验证本地预测及远端插值。
- 运行至少 10 个并发无界面房间，确认服务端不会持续积累 tick 延迟或 WebSocket 缓冲。

## 10. 实施顺序

1. 抽取 RaceSimulation，并通过现有单人测试证明行为兼容。
2. 改造多人控制、AI 控制切换和独立随机流。
3. 实现协议类型、校验器、快照序列化和状态镜像。
4. 实现 WebSocket 服务、RoomManager、加载阶段、重连和服务端比赛循环。
5. 引入 LocalRaceSession 与 OnlineRaceSession，改造主循环依赖。
6. 完成多人入口、大厅、在线暂停和在线结果 UI。
7. 加入本地预测、远端插值、事件去重和网络积压保护。
8. 完成自动化、并发、延迟和部署验收。
9. 更新 README 与架构文档，记录运行方式、环境变量、协议和非目标范围。

## 11. 交付约束

- 不引入前端 bundler 或框架。
- 除服务端 ws 外不增加非必要运行时依赖。
- 服务端专用文件不能被静态服务器公开。
- 不破坏当前坐标系、120 Hz 固定步长、Rng 使用规则和模拟/展示分层。
- 客户端预测不得成为权威状态来源。
- 所有新增网络接口必须版本化并有自动化校验测试。
