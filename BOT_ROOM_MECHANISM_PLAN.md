# Bot 自建房机制（修订版）

## 文档交付

- 实施时在项目根目录新增 `BOT_ROOM_MECHANISM_PLAN.md`。
- 文件内容完整同步本计划，包括机制、Admin 开关、接口、回收策略和测试标准。
- 后续实现若调整行为，必须同步更新该文档，避免设计与代码不一致。

## 总体机制

- 仅作用于多人模式。
- 服务启动后立即检查一次，之后每 15 秒检查是否存在“公开、等待中、未满且可加入”的房间。
- 如果不存在，则创建 1 个 Bot 房；私人房、满员房和比赛中的房间不阻止创建。
- 每个进程最多保留 1 个无人类玩家的空闲 Bot 房。出现其他公开可加入房时，回收多余空闲 Bot 房。
- Bot 房配置：
  - 房间名：`Bot自建房`
  - 房主名：`赛博房主`
  - 公开房、8 个总席位，Bot 占 1 席
  - 创建时随机地图，之后不自动换图
  - 难度为 `normal`，关闭自动补充 AI，其余保持默认
  - 房主的赛车、涂装和头像分别随机
  - Bot 始终为准备状态

## 精简聊天指令

仅保留三类指令，支持中文、英文和单字母缩写：

- 帮助：`/帮助`、`/help`、`/h`
- 修改地图：`/地图 <数字>`、`/map <数字>`、`/m <数字>`
- 开始比赛：`/开始`、`/start`、`/s`

具体规则：

- 同时识别半角 `/` 和全角 `／`。
- 地图参数只接受一个整数；当前地图范围为 `1-6`，编号由服务端按照 `TRACKS` 的固定顺序生成。
- 可将全角数字 `１-６`规范化为半角数字；不接受地图名称、ID、`random` 或额外参数。
- `/h`的回复同时列出当前地图编号与名称，不再提供地图列表、状态等独立指令。
- 无效地图编号提示正确范围且不修改设置；未知斜杠指令返回精简帮助。
- 普通聊天不会触发 Bot 自动回复。
- 所有人类玩家都可以改图和请求开赛；地图修改设置房间级 3 秒冷却。
- 地图修改成功后，清除所有人类玩家的准备状态，Bot 保持准备，并提示玩家重新准备。
- 每名新玩家首次加入时，Bot 发送欢迎消息并提示使用 `/h`；重连不重复欢迎。
- Bot 消息使用服务器内部发送接口，不受玩家 3 秒聊天限频约束，但保留现有 WebSocket 背压保护。

## 开赛、AI 与房间回收

- `/s`要求至少有 1 名人类玩家，且所有保留席位中的人类玩家均已连接并准备。
- 不满足时，Bot 必须发送：`请等待所有人准备`。
- 满足时立即进入赛道加载，加载完成后复用现有权威 3 秒“3、2、1”倒计时，不增加第二段房间倒计时。
- Bot 以 `controllerKind: "ai"`、`aiPlayerNumber: 1`进入比赛，使用现有普通 AI 驾驶系统。
- Bot 自动视为完成加载，不进入输入 ACK、账号活跃、经验、Rating、排行榜或用户比赛结算。
- 允许“1 名人类玩家 + Bot”比赛；普通房仍保持现有至少 2 名人类成员的规则。
- Bot 赛后自动返回房间并恢复准备，不阻塞人类玩家返回。
- 所有人类主动离开比赛时，立即结束结算并销毁房间。
- 所有人类意外断线时保留现有 30 秒重连窗口，并由 takeover AI 接管；窗口到期后仍无人类可恢复，则结束结算并销毁。
- 没有 WebSocket 接收者时继续跳过比赛快照构建。
- 房间销毁后，由下一次 15 秒检查按需补充新的 Bot 房。

## Admin 运行时开关

- Admin 页面新增独立的“服务器设置”面板，不放入统计指标区域。
- 面板包含一个开关：
  - 名称：`BOT_ROOM_ENABLED`
  - 状态文案：`已开启` / `已关闭`
  - 说明：开启后，无公开可加入房间时服务器会自动创建 Bot 房
- 开关修改后立即生效，并持久化到现有 SQLite 数据库。
- 新增轻量 `server_settings` 表，保存已知配置键、值和更新时间。
- 启动时配置优先级：
  1. SQLite 中管理员最后保存的值
  2. 环境变量 `BOT_ROOM_ENABLED`
  3. 默认值 `true`
- 新增 Admin API：
  - `GET /api/admin/settings`：读取当前有效值和配置来源
  - `PATCH /api/admin/settings`：仅接受 `{ "botRoomEnabled": boolean }`
- 写接口继续要求管理员登录、同源 JSON 请求和严格布尔校验。
- 页面登录和手动刷新时加载设置；切换期间禁用控件，失败时恢复服务端原值并显示错误。
- 开启时立即执行一次房间检查，不等待下一个 15 秒周期。
- 关闭时：
  - 立即销毁没有可恢复人类玩家的空闲 Bot 房
  - 停止自动创建和补充新的 Bot 房
  - 有人类玩家的等待中 Bot 房继续显示在大厅，并允许直接加入和快速匹配；比赛中或满员时仍不可加入
  - 房内玩家可以继续游戏和重连
  - 最后一名可恢复的人类玩家离开或过期后自动销毁
- 重新开启时取消现有 Bot 房的待回收状态，并立即重新检查是否需要创建房间。
- 不增加任何 Bot 房统计指标、Admin 统计卡片或相关聚合数据。

## 接口与 UI

- 多人协议继续使用 v5，不增加新的客户端消息类型。
- 增加兼容字段：
  - `lobby_state.rooms[].botManaged`
  - `room_state.botManaged`
  - `room_state.members[].isBot`
  - `room_chat.isBot`
  - 可选 `botMessageKey`、`botMessageArgs`用于客户端本地化
- 大厅和房间成员列表显示 Bot 标识；Bot 聊天显示专用徽章。
- 服务端继续使用中文权威值 `Bot自建房`、`赛博房主`；客户端按当前游戏语言动态显示：中文为 `Bot自建房`、`赛博房主`，英文为 `Bot Room`、`Cyber Host`，切换语言后无需重连即可更新大厅、房间成员、Bot 聊天发送者名称和已有 Bot 消息正文。
- Bot 房主的准备状态不显示 `AI` 前缀，中文显示 `已准备`，英文显示 `READY`。
- Bot 房设置控件保持只读，并提示使用 `/m 数字`改图、`/s`开赛。
- 中文客户端必须显示精确提示`请等待所有人准备`，英文客户端使用对应本地化文本。
- Admin 持久化开关属于服务器配置，不计入在线人数、分析数据或运行指标。

## 测试与验收

- 验证根目录存在 `BOT_ROOM_MECHANISM_PLAN.md`，且内容与批准计划一致。
- 验证 Bot 房间名和房主名的中英文显示，以及切换语言后大厅、房间成员、已有 Bot 聊天发送者名称和消息正文立即更新；中文欢迎消息显示 `欢迎 {displayName} 加入房间！发送 /h 查看指令。`。
- 验证 Bot 房主准备状态中文显示 `已准备`、英文显示 `READY`，不包含 `AI` 前缀。
- 验证三类指令的中文、英文和 `/h`、`/m`、`/s`缩写。
- 验证地图只接受单个数字参数，包括全角数字规范化；名称、ID、随机和多余参数均被拒绝。
- 验证 `/h`能够列出当前全部地图编号，且不再支持状态、地图列表等其他指令。
- 验证改图权限、3 秒房间级冷却、人类准备状态重置和 Bot 准备状态保持。
- 验证单人类加 Bot 能开赛，未全部准备时出现精确提示，并只使用现有赛内 3 秒倒计时。
- 验证 Bot 使用普通 AI、没有输入 ACK，也不参与用户结算和排行榜。
- 验证 Admin 设置接口的认证、同源限制、布尔校验和页面失败回滚。
- 使用同一个 SQLite 文件重建服务器，验证管理员设置可跨重启保留并覆盖环境变量默认值。
- 验证关闭开关立即回收空房、停止补房；现有有人类玩家的等待中 Bot 房仍允许大厅加入、直接加入和快速匹配，并在无人后销毁；重新开启后立即检查是否需要补房。
- 验证没有新增 Bot 相关 Admin 指标或 RuntimeMetrics 字段。
- 运行 `npm test`、`npm run check`和`npm run smoke:multiplayer`；空闲 Bot 房不得进入 60Hz 模拟循环。

## 已确定默认

- 任意玩家均可改图和请求开赛。
- 仅公开可加入房参与兜底判断。
- 地图只在创建房间时自动随机。
- 8 个席位包含 Bot 房主，因此最多容纳 7 名人类玩家。
- Admin 设置立即生效、持久化，并采用“停止补房、现有房允许加入、无人后回收”的关闭策略。
- Bot 采用确定性指令解析，不接入自然语言模型。
