# 多人网络第二阶段：协议 v4 与二进制比赛态优化

> 状态：本地实施与验收完成  
> 开始日期：2026-08-04  
> 实施基线：Git `3d2506c`  
> 范围：120Hz 权威物理、60Hz 服务器 tick、20Hz 完整快照、100ms 插值与 30 秒恢复窗口保持不变。  
> 生产 VPS 对比：延后，不作为第二阶段验收阻塞项。

## 实施进度

- [x] 创建本地第二阶段计划文件并记录实施基线。
- [x] 建立浏览器与 Node 共用的协议 v4 二进制 codec、固定枚举表和黄金字节测试。
- [x] 为每场比赛增加 `wireRaceId`，将服务端完整快照改为共享二进制包。
- [x] 客户端接收 ArrayBuffer 快照并保持预测、插值、ACK 和恢复行为不变。
- [x] 将比赛输入改为固定 28 字节二进制包并完成服务端严格校验。
- [x] 实现 v3 终止提示、4006 关闭语义及 v4 客户端停止重试/刷新提示。
- [x] 扩展 codec、二进制流量和协议拒绝指标。
- [x] 更新架构、README 与部署文档。
- [x] 完成全量测试、语法检查、多人烟测和本地 v3/v4 A/B。
- [x] 写入最终实测数据并完成第二阶段本地验收。

## 摘要

- 协议直接由 v3 升级到 v4，不保留 v3 游戏兼容层。
- 服务端快照和客户端输入改为二进制；Lobby、房间、认证、事件、结果等低频消息继续使用 JSON v4。
- 不引入增量快照、动态降频、`perMessageDeflate`、多进程分片或新运行时依赖。
- JSON 事件和结果保持可靠、不可跳过；二进制快照仍是完整替换状态，可因背压跳过。

## 协议与接口

- `PROTOCOL_VERSION = 4`，二进制 codec revision 为 `1`。
- 二进制包统一使用小端序和固定前导：魔数 `TLG4`、codec revision、packet type、`uint16` 总包长。
- `prepare_race` 增加非零随机 `wireRaceId: uint32`；字符串 `raceId` 继续用于 JSON 业务消息。
- 输入包固定 28 字节，包含 `wireRaceId`、`seq`、`useItemSeq`、16 位油门/刹车/转向及漂移/回看标志。
- 快照包最大 65,535 字节，包含完整比赛头、车辆、投射物、障碍物、道具箱和真人玩家 ACK。
- 世界状态和无可靠边界值使用 `Float32`；角度使用归一化 `Int16`；短计时器使用毫秒 `Uint16`；比赛时间使用毫秒 `Uint32`；布尔值使用位图；稳定枚举使用 `Uint8`。
- nullable 字段使用固定哨兵值；除角度归一化外不得静默截断或钳制权威状态。
- v3 消息返回 `client_update_required` 并以 4006 关闭；v4 客户端把 4006 和协议损坏视为终止错误并提示刷新。

## 实施变更

### Codec 与快照

- 建立零依赖共享 codec，固定枚举数值，不依赖对象枚举顺序。
- 服务端遍历权威模拟状态写入可复用 scratch writer；scratch 按需增长但硬限制 64KiB。
- 每个快照生成一个不可变输出包并在房间内共享；不执行快照 `JSON.stringify`。
- ACK 只包含真实房间成员，不再为 AI 发送无意义 ACK。
- 无接收者时继续权威模拟并跳过编码；恢复时发送一次当前完整二进制快照。

### 客户端与输入

- WebSocket 设置 `binaryType = "arraybuffer"`；字符串帧进入 JSON 流程，二进制帧进入快照 codec。
- 二进制快照应用到现有 Kart、实体、预测和插值逻辑，不改变游戏性参数。
- 输入在现有渲染帧调度点编码；仅在 WebSocket 接受发送后推进 seq 和输入历史。
- 服务端只接受合法的 v4 input 二进制包，其他客户端二进制帧关闭连接。

### 指标、文档与发布

- `/api/metrics` 和结构化日志增加快照编码耗时、二进制大小、输入解码耗时、codec 错误、超大包、v3 拒绝和非法二进制计数。
- 更新架构、README 和部署说明，采用 v4 原子发布；旧页面需要刷新，异常时整体回滚到上一镜像。

## 测试与验收

- Codec：黄金字节、往返一致性、边界、nullable、枚举、量化误差以及损坏/截断包。
- 容量：最多 8 辆车、64 个投射物、64 个障碍物和 255 个道具箱。
- 协议：v3 终止、v4 正常、错误 `wireRaceId`、旧 tick、恢复顺序和背压跳过恢复。
- 游戏体验：操控、火箭起步、漂移、道具、碰撞、排名、AI 接管和预测校正无退化。
- 频率：物理 120Hz、tick 60Hz、客户端快照 19.5～20.5Hz。
- 本地 A/B：
  - 当前标准 8 车 JSON 快照约 2,895B；v4 目标不超过 1,536B。
  - 当前典型 JSON 输入约 155B；v4 输入固定 28B。
  - 8 人标准比赛总线流量至少降低 50%，实体密集快照至少降低 55%。
  - tick p99 不得比 v3 基线增加超过 0.2ms；`catchUpCapped`、房间错误和 codec 错误均为 0。
  - 60 秒持续负载后 RSS、heap 和 arrayBuffers 不得呈线性增长。
- 完整运行 `npm test`、`npm run check`、`npm run smoke:multiplayer` 和新增本地 A/B。

## 默认假设

- 现代浏览器支持 WebSocket 二进制帧、ArrayBuffer 和 DataView。
- 第二阶段直接切换 v4，部署期间允许旧页面要求用户刷新。
- 不新增生产 VPS 测试，不改动第一阶段的生产对比待办。

## 实测记录

| 时间 | 阶段 | 命令/场景 | 结果 |
| --- | --- | --- | --- |
| 2026-08-04 | 初始化 | 创建本文件并记录 Git `3d2506c` | 完成 |
| 2026-08-04 | v3 本地基线 | 标准 8 车初始完整 JSON 快照 | 2,895B |
| 2026-08-04 | v3 本地基线 | 典型 JSON 输入 | 155B |
| 2026-08-04 | Codec | `node --test tests\binary-race-codec.test.js` | 6/6 通过；标准 8 车 1,173B，最大 8/64/64/255 容量与损坏包拒绝通过 |
| 2026-08-04 | 全量测试 | `npm test` | 294/294 通过 |
| 2026-08-04 | 语法/导入检查 | `npm run check` | 39/39 浏览器模块导入通过 |
| 2026-08-04 | 多人烟测 | `npm run smoke:multiplayer` | Lobby、活动房间、重连、私房认证、二进制输入/快照均通过；codec 错误 0 |
| 2026-08-04 | 8 人实时 A/B | v3 基线提交 `3d2506c`，10 秒稳定负载 | 快照 3,233.99B；输入 142.20B；tick p99 0.7611ms |
| 2026-08-04 | 8 人实时 A/B | 当前 v4，60 秒持续负载 | 快照 1,251B；输入 28B；快照 20.014Hz；tick p99 0.5868ms |
| 2026-08-04 | A/B 结论 | `npm run ab:multiplayer-phase2` | 快照 -61.32%，输入 -80.31%，总线流量 -62.70%，tick p99 差值 -0.1743ms |
| 2026-08-04 | 稳定性 | v4 60 秒、最后 15 秒平台区间 | RSS 波动 679,936B；heap 489,584B；arrayBuffers 36,455B；无持续线性增长 |
| 2026-08-04 | 错误指标 | v4 60 秒持续负载 | `catchUpCapped=0`、房间错误 0、codec 错误 0 |

## 本地验收结论

- 协议已原子切换到 v4；120Hz 权威物理、60Hz tick、20Hz 完整快照、100ms 插值和 30 秒恢复窗口未改变。
- v3 客户端收到 `client_update_required` 并以 4006 关闭；v4 客户端在版本或 codec 损坏时停止自动重连并提供刷新操作。
- 标题页与单机懒连接、输入帧调度、暂停保活、道具边沿、预测/ACK 清理和恢复顺序继续由全量测试覆盖，未发现回归。
- 本地 8 人 A/B 已超过带宽目标，tick p99 未退化，60 秒负载没有调度、房间或 codec 错误。
- 生产 VPS 上线前后指标对比继续延后为独立任务，不影响第二阶段本地完成状态。
